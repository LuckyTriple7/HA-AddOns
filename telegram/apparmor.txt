#include <tunables/global>

profile telegram_addon flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  capability chown,
  capability dac_override,
  capability fowner,
  capability setgid,
  capability setuid,
  file,
  signal,
  network tcp,
  network udp,
  network inet,
  network inet6,
  network netlink raw,
  network unix dgram,

  /run.sh ix,
  /run/{,**} rwk,
  /dev/tty rw,
  /bin/** ix,
  /usr/bin/** ix,
  /usr/local/bin/** ix,
  /usr/lib/** ix,
  /sbin/** ix,
  /usr/sbin/** ix,
  /var/run/** mrwkl,
  /var/run/ mrwkl,
  /dev/* mrwkl,
  /tmp/** mrkwl,

  /ui/** rwix,
  /data/** rw,
  /config/** rw,

  ptrace (trace,read) peer=docker-default,
  signal (receive) set=(kill,term) peer=/usr/bin/docker,
}
