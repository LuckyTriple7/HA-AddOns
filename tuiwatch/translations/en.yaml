configuration:
  username:
    name: Username
    description: Username for the web interface (only relevant for direct access on port 17794 — behind HA Ingress, HA handles authentication).
  password:
    name: Password
    description: Password for the web interface (only relevant for direct access on port 17794). Please change it!
  session_hours:
    name: Session duration (hours)
    description: How many hours a login session stays valid. Default 24.
  poll_interval:
    name: Check interval (seconds)
    description: "Delay between two automatic price checks. Travel prices change slowly — default 21600 (6 hours), minimum 600 (10 minutes). Please keep it fair, not every minute."
  market_trend_threshold:
    name: Market trend threshold (%)
    description: "Cumulative price movement over 14 days above which the market trend counts as rising/falling instead of stable. Default 1.0 — a smaller value reacts to smaller moves."
  trippilot_home_location:
    name: TripPilot home location (ZIP/city)
    description: "Pre-fills the TripPilot question 'Where are you starting from?' (for car/bus/train arrival or day trips). Leave empty to disable — can always be changed in the questionnaire itself."
  ha_sensors:
    name: Home Assistant sensors
    description: "Create a sensor (sensor.tuiwatch_<hotelname>) in Home Assistant per tracked offer. State = current price in €, 'unavailable' on error; trip details are in the 'description' attribute. Default on."
  notify_ha:
    name: HA notifications
    description: "Show a persistent Home Assistant notification on price change or when a target price is reached. Default on."
  notify_price_change:
    name: Notify on every price change
    description: "Notify whenever a price changes (up/down). Target-price notifications are always sent regardless. Default on."
  notify_cheaper_date:
    name: Cheaper-date alert
    description: "Notify when the price calendar shows another departure date (also outside your range) that is clearly cheaper than your tracked price. Default on."
  cheaper_date_min_diff:
    name: Minimum saving for date alert (€)
    description: "Minimum saving per person before the cheaper-date alert triggers. Default 50 €."
  notify_calendar_trend:
    name: Calendar price change
    description: "Notify when a price in an offer's price calendar changes for an already-known travel date (hotel name + affected month, no details). Default on."
  notify_errors:
    name: Sold-out / error alert
    description: "Notify when an offer returns no result several times in a row (sold out or outdated URL). Default on."
  notify_api_errors:
    name: API outage alert
    description: "Notify when the API self-test finds a critical TUI endpoint failing (TUI may have changed the API), and send an all-clear once everything works again. Default on."
  notify_booked_drop:
    name: Cheaper-than-booked alert
    description: "Notify when the price drops below the booked price you entered after booking (rebooking might be worth it). Default on."
  booked_drop_min_diff:
    name: Min. saving below booked price (€)
    description: "Minimum saving per person before the cheaper-than-booked alert fires. Default 50 €."
  digest_enabled:
    name: Send weekly digest
    description: "Send a weekly summary (biggest drops, new lows, offers below target price) via Telegram and/or e-mail. Requires Telegram or SMTP. Default off."
  digest_weekday:
    name: Digest weekday
    description: "Day of the week the weekly digest is sent: 1 = Monday … 7 = Sunday. Default 1 (Monday)."
  notify_aktionscodes:
    name: Promo code alert
    description: "Notify when new public TUI promo codes appear. Default on."
  aktionscode_min:
    name: Minimum promo code value (€)
    description: "Only report promo codes from this value upwards (0 = all). Default 0."
  aktionscode_interval:
    name: Promo code check interval (seconds)
    description: "How often the public promo codes are checked. Default 21600 (6 hours), minimum 1800."
  auto_backup:
    name: Automatic backup
    description: "Store a full backup (offers incl. history, trips incl. PDF, saved searches) as a ZIP under /addon_config/backups once a week — survives reinstalling the add-on. Default on."
  auto_backup_keep:
    name: Auto backups to keep (count)
    description: "How many automatic backups are kept; older ones are deleted. Default 5."
  telegram_bot_token:
    name: Telegram bot token
    description: "Token of a Telegram bot (format 123456789:ABC-...) to receive notifications via Telegram. Leave empty to disable Telegram. Create a bot via @BotFather."
  telegram_chat_id:
    name: Telegram chat ID
    description: "Chat ID of the recipient for Telegram messages. Find it via @userinfobot or by sending /start to the bot."
  smtp_host:
    name: SMTP server
    description: "Mail server for sending the offers e-mail (e.g. smtp.gmail.com). Leave empty to disable e-mail."
  smtp_port:
    name: SMTP port
    description: "Mail server port. 587 for STARTTLS (default), 465 for SSL."
  smtp_user:
    name: SMTP user
    description: "Username/login for the mail server (often the e-mail address)."
  smtp_password:
    name: SMTP password
    description: "Password or app password for the mail server."
  smtp_from:
    name: Sender address
    description: "Visible sender address. Empty = the SMTP user is used."
  smtp_to:
    name: Default recipient
    description: "Pre-filled recipient for the offers e-mail (can be changed in the UI before sending)."
  smtp_tls:
    name: Use STARTTLS
    description: "On = STARTTLS (port 587). Off = direct SSL/TLS (port 465)."
  nc_addressbook_url:
    name: Nextcloud address book URL
    description: "Full CardDAV URL of the address book, as offered for copying in Nextcloud's Contacts app (e.g. https://cloud.example.com/remote.php/dav/addressbooks/users/user/contacts/). Leave empty = no address book, e-mail recipient stays free text."
  nc_user:
    name: Nextcloud username
    description: "Nextcloud username for accessing the address book."
  nc_app_password:
    name: Nextcloud app password
    description: "App password (not the regular login password!) — create one under Nextcloud settings → Security → Devices & sessions → Create new app password."
  anthropic_api_key:
    name: Anthropic API key
    description: "API key for Claude (console.anthropic.com), enables the 'AI summary' in the hotel search (location, rooms, restaurants, pool, amenities). Leave empty = feature off."
  anthropic_model:
    name: Claude model for the AI summary
    description: "Which Claude model is used for the hotel summary. Opus = default, very thorough. Sonnet = faster/cheaper at nearly the same quality. Haiku = cheapest, shorter. Fable = most capable model, priciest call."
  ai_provider:
    name: AI provider
    description: "Which AI provider is used for ALL AI features (AI summary, comparison, TripPilot/day trip, auto-tags, portfolio question). anthropic = Claude (default), gemini = Google Gemini (e.g. for Google Search grounding). Needs the matching API key below."
  gemini_api_key:
    name: Gemini API key
    description: "API key for Google Gemini (aistudio.google.com), only relevant when 'AI provider' = gemini. Leave empty = feature off."
  gemini_model:
    name: Gemini model
    description: "Which Gemini model is used when 'AI provider' = gemini. 3.1 Pro = flagship, very thorough. 3.5 Flash = balanced, faster/cheaper. 2.5 Flash = cheapest."
  ai_max_web_searches:
    name: Max. web searches per AI call
    description: "Caps how many times Claude may search the web on its own during a single AI call (summary, comparison, travel advisor/TripPilot, day trip). More searches = more thorough, but more input tokens and higher cost. Default 12 — e.g. 8 for leaner, 20 for very thorough with several destinations/hotels. Only applies to Anthropic/Claude — Gemini has no limit on the number of web searches."
  verbose_log:
    name: Verbose logging
    description: Logs more detail per check. Default off — only errors and important events are logged.
