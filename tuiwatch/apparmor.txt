#include <tunables/global>

profile tuiwatch_addon flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  capability chown,
  capability dac_override,
  capability fowner,
  capability setgid,
  capability setuid,
  capability sys_admin,
  file,
  signal,
  network tcp,
  network udp,
  network inet,
  network inet6,
  network netlink raw,
  network unix dgram,

  /run.sh ix,
  /run/{,**} rwk,
  /dev/tty rw,
  /bin/** ix,
  /usr/bin/** ix,
  /usr/local/bin/** ix,
  /usr/lib/** ix,
  /sbin/** ix,
  /usr/sbin/** ix,
  /var/run/** mrwkl,
  /var/run/ mrwkl,
  /dev/* mrwkl,
  /dev/shm/** rw,
  /tmp/** mrkwl,

  /app/** rwix,
  /data/** rw,
  /config/** rw,

  ptrace (trace,read) peer=docker-default,
  signal (receive) set=(kill,term) peer=/usr/bin/docker,
}
