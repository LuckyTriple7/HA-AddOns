flask==3.1.3
requests==2.34.2
itsdangerous==2.2.0
dnspython==2.8.0
# Verschlüsselt SMTP-Passwort/Telegram-Token in settings.json (settings.py).
# Eigener Eintrag statt sich auf aioquics transitive Abhängigkeit zu
# verlassen — sonst verschwindet die Verschlüsselung stillschweigend, sollte
# aioquic je wieder rausfliegen. abi3+musllinux-Wheel, keine Kompilierung.
cryptography==50.0.1
# QUIC/HTTP3-Check. Zieht zusätzlich pylsqpack, service_identity — beide
# haben abi3+musllinux-Wheels (geprüft für Python 3.14 auf Alpine, amd64
# und aarch64), keine Kompilierung im Image nötig.
aioquic==1.3.0
