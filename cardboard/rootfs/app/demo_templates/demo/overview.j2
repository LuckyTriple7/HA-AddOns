{%- set date = now().strftime('%d.%m.%Y') -%}
{%- set time = now().strftime('%H:%M') -%}
{%- set version = states('sensor.current_version') | default('–') -%}
{%- set uptime  = states('sensor.ha_uptime') | default('–') -%}

## 🏠 CardBoard Demo

**Datum:**   {{ date }}
**Uhrzeit:** {{ time }}

---

## ⚙️ Home Assistant

  Version:  {{ version }}
  Laufzeit: {{ uptime }}

---

## 🌡️ Beispiel Sensoren

Passe diese Datei an deine Sensoren an:
  `/config/addons_config/cardboard/demo/overview.j2`

Beispiel Sensor:
  Temperatur: {{ states('sensor.dein_temperatursensor') | default('nicht konfiguriert') }}

Beispiel Binärsensor:
  Bewegung: {% set b = states('binary_sensor.dein_bewegungsmelder') %}
  {%- if b == 'on' %}🔴 Aktiv{% elif b == 'off' %}🟢 Inaktiv{% else %}– nicht konfiguriert{% endif %}
